/* escape a quote from considering it as an end of string quote by
using backslash \ in front of the quote. */
var myStr = "I am a \"double quoted\" string inside \"double quotes\"."; // Change this line
