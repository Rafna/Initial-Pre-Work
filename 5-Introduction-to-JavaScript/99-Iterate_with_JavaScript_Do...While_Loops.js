/*Iterate using do-while loop. This is used when the code should be execute
atleast once. */
// Setup
var myArray = [];
var i = 10;

// Only change code below this line.
do {
  myArray.push(i);
  i++;
} while(i < 5);
