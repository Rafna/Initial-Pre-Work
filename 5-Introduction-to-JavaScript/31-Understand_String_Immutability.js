/* String are immutable that means they cannot be changed. Here the first
character of myStr cannot be changed individually. It can be Changed
only by changing the entire string. */
// Setup
var myStr = "Jello World";

// Only change code below this line

myStr = "Hello World"; // Fix Me
