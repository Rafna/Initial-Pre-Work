/* When a + operator is used with string value, it is called concatenation.
We can build new string with concatenating other strings. */
// Example
var ourStr = "I come first. " + "I come second.";

// Only change code below this line

var myStr = "This is the start. " + "This is the end.";
