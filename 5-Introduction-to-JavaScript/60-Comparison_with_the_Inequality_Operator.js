/* Inequality operator != is the opposite of equality operator. It also performs
data type conversion while comparing. */
// Setup
function testNotEqual(val) {
  if (val != 99) { // Change this line
    return "Not Equal";
  }
  return "Equal";
}

// Change this value to test
testNotEqual(10);
