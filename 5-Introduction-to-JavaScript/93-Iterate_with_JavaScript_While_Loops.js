/*Iterate with while loop. Same code will be executed until the condition
 in the while is true. */
 // Setup
var myArray = [];

// Only change code below this line.
var i = 0;
while(i < 5) {
  myArray.push(i);
  i++;
}
