/*Comparison done with if/else statement can be replaced with comparison
operators in return statement. */
function isLess(a, b) {
  // Fix this code
  return a < b;
}

// Change these values to test
isLess(10, 15);
